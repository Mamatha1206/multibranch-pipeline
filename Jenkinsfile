pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo "Building on dev branch"
            }
        }
        stage('Test') {
            steps {
                echo "Running tests on dev branch"
            }
        }
    }
}
